# Database Configuration
# Replace with your actual database credentials
DATABASE_URL=postgresql://username:password@localhost:5432/burgercreativity
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=burgercreativity

# Server Configuration
PORT=5000
NODE_ENV=development

# Add any additional environment variables needed by your application below